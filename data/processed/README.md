# Processed Data

`exercises_clean.csv` is the validated dataset used by the optimization model.

It contains one row per exercise-muscle record and includes stable exercise IDs during model construction when display names are duplicated.
